# Mohamed Khalel Portfolio

A modern, interactive portfolio UI with light and dark themes, animated sections, and project cards.

## Getting Started

- Install dependencies: `npm install`
- Run the dev server: `npm run dev`
- Build for production: `npm run build`
- Preview the build: `npm run preview`

## Customize

- Replace the profile placeholder in `src/App.jsx` with a real photo.
- Update project cards and live/GitHub links in `src/App.jsx`.
- Adjust colors and typography in `src/index.css`.

## Image Credits

- Project thumbnails are loaded from Unsplash images.
	https://images.unsplash.com/
